accelerate==1.13.0
codetiming>=1.4
datasets>=2.19
deepspeed==0.19.1
fastapi>=0.110,<1
hydra-core>=1.3
numpy<2
omegaconf>=2.3
pandas>=2.0
peft==0.14
pyarrow>=15
ray[default]>=2.10
requests>=2.31
sentence-transformers>=3,<6
tensordict>=0.5
# torch>=2.1
tqdm>=4.66
transformers==4.57.6
uvicorn[standard]>=0.27,<1
wandb>=0.17

# Install exactly one FAISS build that matches your CUDA/CPU environment.
# faiss-cpu

# VERL rollout also requires a CUDA-compatible vLLM install.
# The copied TrajRL VERL fork has customized wrappers for vLLM <= 0.6.3.
# Install the vLLM build that matches the CUDA/PyTorch stack in the training env.
# vllm
